#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle config set path --local vendor/bundle
bundle config set with --local test
bundle install

# Do any other automated setup that you need to do here
